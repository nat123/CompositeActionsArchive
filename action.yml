name: 'sum'
description: 'sum of numbers'
inputs:
 destinationFolder: #path
   description: "sum"
   required: true
   default: ~
runs:
  using: "composite"
   steps:
     run: |
          chmod +x ${{ github.action_path }}/sum.sh
          ${{ github.action_path }}/sum.sh 
     
     
    
    
 
